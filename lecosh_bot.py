import logging
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import Application, CommandHandler, CallbackQueryHandler, ContextTypes

# ⚠️ ВСТАВЬ СЮДА СВОЙ ТОКЕН (только в этот файл, никому не показывай)
TOKEN = "ВСТАВЬ_ТОКЕН_СЮДА"

logging.basicConfig(level=logging.INFO)

# ─────────────────────────────────────────
# ТЕКСТЫ
# ─────────────────────────────────────────

TEXT_START = """🕯 Добро пожаловать в *l'ecosh*!

Привет! Я помогу вам выбрать идеальную свечу ручной работы.

Мы создаём свечи из натуральных материалов с заботой о вас и природе 🌿

Выберите, что вас интересует:"""

TEXT_CATALOG = """🕯 *Каталог l'ecosh*

Выберите категорию:"""

TEXT_AROMA = """🌸 *Свечи по аромату*

Выберите настроение:"""

TEXT_SIZE = """📏 *Свечи по размеру*

Выберите формат:"""

TEXT_GIFTS = """🎁 *Подарочные наборы*

Создайте идеальный подарок для близкого человека!
Каждый набор упакован в крафтовую коробку с открыткой 🤍

Выберите набор:"""

TEXT_NEW = """🆕 *Новинки*

Наши последние коллекции — первыми узнайте о новых ароматах!

📦 Скоро здесь появятся новинки. Следите за обновлениями!"""

TEXT_CANDLE_FOREST = """🕯 *Свеча «Утренний лес»*

🌲 Аромат: кедр, можжевельник, нотки мха
📏 Объём: 200 мл | Время горения: ~40 часов
🌿 Состав: соевый воск, хлопковый фитиль, натуральные ароматизаторы

💰 Цена: 890 ₽

✨ Идеальна для утреннего ритуала или медитации"""

TEXT_CANDLE_VANILLA = """🕯 *Свеча «Ванильный уют»*

🍪 Аромат: ваниль, тёплая карамель, сливки
📏 Объём: 180 мл | Время горения: ~35 часов
🌿 Состав: соевый воск, хлопковый фитиль, натуральные ароматизаторы

💰 Цена: 790 ₽

✨ Согревает и создаёт уют в любом пространстве"""

TEXT_CANDLE_ROSE = """🕯 *Свеча «Цветущий сад»*

🌹 Аромат: роза, пион, лёгкая зелень
📏 Объём: 200 мл | Время горения: ~40 часов
🌿 Состав: соевый воск, хлопковый фитиль, натуральные ароматизаторы

💰 Цена: 890 ₽

✨ Нежный цветочный аромат для романтического вечера"""

TEXT_ORDER = """🛍 *Как оформить заказ*

Всё просто:

1️⃣ Выберите свечу в каталоге
2️⃣ Нажмите «Оплатить»
3️⃣ Напишите нам — подтвердим заказ
4️⃣ Пришлём реквизиты для оплаты
5️⃣ После оплаты упакуем и отправим 🕯

💬 Есть вопросы? Напишите — ответим быстро!"""

TEXT_PAYMENT = """💳 *Способы оплаты*

Мы принимаем:

💳 Перевод на карту
📱 СБП (Система быстрых платежей)
💰 Наличными при самовывозе

После выбора товара напишите нам — пришлём реквизиты.
Подтверждение отправим сразу после оплаты ✅"""

TEXT_DELIVERY = """📦 *Доставка*

🚚 Доставляем по всей России!

📦 СДЭК — от 2 до 7 дней
📬 Почта России — от 5 до 14 дней
🏃 Самовывоз — уточните адрес у менеджера

💝 Упаковываем каждую свечу с любовью!
⏱ Срок сборки заказа: 1–2 рабочих дня"""

TEXT_FAQ = """❓ *Частые вопросы*

Выберите вопрос:"""

TEXT_FAQ_COMPOSITION = """🌿 *Состав наших свечей*

Мы используем только натуральные материалы:

🌿 Соевый воск — экологичный, долго горит
🪢 Хлопковый фитиль — без свинца и цинка
🌸 Натуральные ароматизаторы — без синтетики

Мы против вредных компонентов и тестирования на животных 🐾"""

TEXT_FAQ_CARE = """💡 *Уход за свечой*

Несколько простых правил:

✂️ Подрезайте фитиль до 5 мм перед каждым поджиганием
⏱ Первый раз жгите не менее 2 часов
🌬 Не оставляйте свечу на сквозняке
🕯 Не жгите менее 1 часа — воск не успеет растопиться равномерно
🔥 Не допускайте горения менее 0,5 см до дна"""

TEXT_FAQ_BURN = """⏱ *Сколько горит свеча?*

Время горения зависит от объёма:

180 мл — около 35 часов
200 мл — около 40 часов
300 мл — около 60 часов

При правильном уходе свеча прослужит дольше 🕯"""

TEXT_FAQ_CUSTOM = """✏️ *Свечи на заказ*

Да! Мы делаем свечи на заказ:

🎨 Индивидуальный аромат
📦 Персональная упаковка и открытка
🏷 Брендирование для бизнеса

Напишите нам — обсудим детали!"""

TEXT_CONTACTS = """💬 *Связаться с нами*

Мы рады ответить на любые вопросы!

📸 Instagram: @lecosh\\_candles
💙 ВКонтакте: vk.ru/lecosh.candles

Напишите нам прямо здесь — обычно отвечаем в течение нескольких часов 🤍"""

TEXT_PAY = """💳 *Оплата заказа*

Для оформления заказа напишите нам — мы подберём удобный способ оплаты и пришлём реквизиты.

📸 Instagram: @lecosh\\_candles
💙 ВКонтакте: vk.ru/lecosh.candles"""

# ─────────────────────────────────────────
# КЛАВИАТУРЫ
# ─────────────────────────────────────────

def kb_main():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("🕯 Каталог", callback_data="catalog"),
         InlineKeyboardButton("🛍 Как заказать", callback_data="order")],
        [InlineKeyboardButton("💳 Оплата", callback_data="payment"),
         InlineKeyboardButton("📦 Доставка", callback_data="delivery")],
        [InlineKeyboardButton("❓ FAQ", callback_data="faq"),
         InlineKeyboardButton("💬 Написать нам", callback_data="contacts")],
        [InlineKeyboardButton("📸 Instagram", url="https://www.instagram.com/lecosh_candles"),
         InlineKeyboardButton("💙 ВКонтакте", url="https://vk.ru/lecosh.candles")],
    ])

def kb_catalog():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("🌸 По аромату", callback_data="cat_aroma"),
         InlineKeyboardButton("📏 По размеру", callback_data="cat_size")],
        [InlineKeyboardButton("🎁 Подарочные наборы", callback_data="cat_gifts"),
         InlineKeyboardButton("🆕 Новинки", callback_data="cat_new")],
        [InlineKeyboardButton("⬅️ Главное меню", callback_data="start")],
    ])

def kb_aroma():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("🌹 Цветочные", callback_data="candle_rose"),
         InlineKeyboardButton("🌲 Древесные", callback_data="candle_forest")],
        [InlineKeyboardButton("🍪 Уютные (ваниль)", callback_data="candle_vanilla")],
        [InlineKeyboardButton("⬅️ Назад", callback_data="catalog")],
    ])

def kb_size():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("🕯 Маленькая 180 мл — от 790 ₽", callback_data="candle_vanilla")],
        [InlineKeyboardButton("🕯 Средняя 200 мл — от 890 ₽", callback_data="candle_forest")],
        [InlineKeyboardButton("⬅️ Назад", callback_data="catalog")],
    ])

def kb_gifts():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("🎁 Набор 2 свечи — 1 490 ₽", callback_data="pay")],
        [InlineKeyboardButton("🎁 Набор 3 свечи — 2 190 ₽", callback_data="pay")],
        [InlineKeyboardButton("💝 Премиум-набор — 3 500 ₽", callback_data="pay")],
        [InlineKeyboardButton("⬅️ Назад", callback_data="catalog")],
    ])

def kb_candle():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("💳 Оплатить", callback_data="pay")],
        [InlineKeyboardButton("⬅️ Назад в каталог", callback_data="catalog")],
    ])

def kb_back_main():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("⬅️ Главное меню", callback_data="start")],
    ])

def kb_back_catalog():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("⬅️ Назад", callback_data="catalog")],
    ])

def kb_faq():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("🌿 Из чего сделаны свечи?", callback_data="faq_composition")],
        [InlineKeyboardButton("💡 Как ухаживать?", callback_data="faq_care")],
        [InlineKeyboardButton("⏱ Сколько горит свеча?", callback_data="faq_burn")],
        [InlineKeyboardButton("✏️ Можно ли на заказ?", callback_data="faq_custom")],
        [InlineKeyboardButton("⬅️ Главное меню", callback_data="start")],
    ])

def kb_contacts():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("📸 Instagram", url="https://www.instagram.com/lecosh_candles"),
         InlineKeyboardButton("💙 ВКонтакте", url="https://vk.ru/lecosh.candles")],
        [InlineKeyboardButton("⬅️ Главное меню", callback_data="start")],
    ])

# ─────────────────────────────────────────
# ОБРАБОТЧИКИ
# ─────────────────────────────────────────

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(TEXT_START, parse_mode="Markdown", reply_markup=kb_main())

async def button(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    d = query.data

    routes = {
        "start":           (TEXT_START, kb_main()),
        "catalog":         (TEXT_CATALOG, kb_catalog()),
        "cat_aroma":       (TEXT_AROMA, kb_aroma()),
        "cat_size":        (TEXT_SIZE, kb_size()),
        "cat_gifts":       (TEXT_GIFTS, kb_gifts()),
        "cat_new":         (TEXT_NEW, kb_back_catalog()),
        "candle_forest":   (TEXT_CANDLE_FOREST, kb_candle()),
        "candle_vanilla":  (TEXT_CANDLE_VANILLA, kb_candle()),
        "candle_rose":     (TEXT_CANDLE_ROSE, kb_candle()),
        "order":           (TEXT_ORDER, kb_back_main()),
        "payment":         (TEXT_PAYMENT, kb_back_main()),
        "delivery":        (TEXT_DELIVERY, kb_back_main()),
        "faq":             (TEXT_FAQ, kb_faq()),
        "faq_composition": (TEXT_FAQ_COMPOSITION, kb_faq()),
        "faq_care":        (TEXT_FAQ_CARE, kb_faq()),
        "faq_burn":        (TEXT_FAQ_BURN, kb_faq()),
        "faq_custom":      (TEXT_FAQ_CUSTOM, kb_faq()),
        "contacts":        (TEXT_CONTACTS, kb_contacts()),
        "pay":             (TEXT_PAY, kb_contacts()),
    }

    if d in routes:
        text, keyboard = routes[d]
        await query.edit_message_text(text, parse_mode="Markdown", reply_markup=keyboard)

# ─────────────────────────────────────────
# ЗАПУСК
# ─────────────────────────────────────────

def main():
    app = Application.builder().token(TOKEN).build()
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CallbackQueryHandler(button))
    print("🕯 Бот l'ecosh запущен! Нажми Ctrl+C чтобы остановить.")
    app.run_polling()

if __name__ == "__main__":
    main()
